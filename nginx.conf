server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    location /mpd/ {
        proxy_pass http://mpd-bridge:8766/;
        proxy_set_header Host $host;
    }
}
